# Library Platform - Микросервисная система электронной библиотеки

Микросервисная система для управления электронной библиотекой, построенная на FastAPI и PostgreSQL.

## 🏗️ Архитектура

Система состоит из трех основных сервисов:

- **User Service** (`:8001`) - управление пользователями
- **Library Service** (`:8002`) - управление книгами и личными библиотеками
- **PostgreSQL** (`:5432`) - база данных

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
# Клонируем основной репозиторий с submodules
git clone --recurse-submodules https://github.com/your-username/library-platform.git
cd library-platform
docker-compose up --build

# Или если уже клонировали без submodules:
git submodule update --init --recursive
```
